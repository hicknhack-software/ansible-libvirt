{
{% for x in ((ansible_local | default({})).libvirt_machines | default({})).items()
         if not x[0] in libvirt_vm_machine_ids %}
{{ x[0] | to_nice_json }}:{{ x[1] | to_nice_json }}{{ '' if loop.last else ',' }}
{% endfor %}
}
